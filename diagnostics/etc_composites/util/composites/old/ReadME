CODES

main.py
-- main code to create netcdf files for each different track, reads in the data and grabs a large enough box around the cyclone center
-- needed inputs are the mat datacycs

datacycs.py 
-- code that processes the datacys and sets grid lens 

dataload.py
-- loads in the model data, and majority of the processing code is done here

get_model_data.py
-- code that loads in the var_data and full_date need in dataload.py
-- created a separate file for easy editting of different models


Ciruclar grid average
-- code to average using angle (0-360 split by 25 degrees) and R (0-1500 km by 100km)
